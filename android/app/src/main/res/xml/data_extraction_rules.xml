<?xml version="1.0" encoding="utf-8"?>
<!--
    MASVS-STORAGE-2: Disable all cloud and device-transfer backups.
    Prevents sensitive vault data, FlutterSecureStorage keys, and database
    files from being exfiltrated through Android's backup mechanism.
    Applies on Android 12+ (API 31+); fullBackupContent="false" handles older.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database"   path="." />
        <exclude domain="file"       path="." />
        <exclude domain="external"   path="." />
        <exclude domain="root"       path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database"   path="." />
        <exclude domain="file"       path="." />
        <exclude domain="external"   path="." />
        <exclude domain="root"       path="." />
    </device-transfer>
</data-extraction-rules>
